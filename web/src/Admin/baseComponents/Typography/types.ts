import React from 'react';

export type TypographyType = {
    bold?: string;
    variant?: string;
    className?: string;
    children: React.ReactNode;
    displayBlock?: boolean;
    light?: boolean;
};
